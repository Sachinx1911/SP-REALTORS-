*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# public/build IS committed on purpose: cPanel has no Node, so the compiled
# CSS/JS must ship with the repo or the live site loads unstyled.
# Run `npm run build` and commit the result before deploying.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
